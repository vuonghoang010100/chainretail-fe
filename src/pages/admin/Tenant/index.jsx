import React from "react";
import ShortContent from "@/components/layout/ShortContent";

const Tenant = () => {
  console.log(import.meta.env.VITE_TEST);

  return <ShortContent>Tenant Admin App</ShortContent>;
};

export default Tenant;
